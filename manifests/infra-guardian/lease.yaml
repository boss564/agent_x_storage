# Infra-Guardian — coordination.k8s.io Lease (T-S1a/T-S2b harness)
# Apply: kubectl apply -f manifests/infra-guardian/lease.yaml
apiVersion: coordination.k8s.io/v1
kind: Lease
metadata:
  name: regime-swarm-leader
  namespace: regime-swarm-shadow
  labels:
    app.kubernetes.io/component: infra-guardian
    app.kubernetes.io/part-of: regime-swarm
spec:
  leaseDurationSeconds: 15
