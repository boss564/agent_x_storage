# Regime Swarm — StatefulSet (monitoring only; Active-Passive via leader ordinal 0)

nameOverride: ""
fullnameOverride: ""

# 1 = active only; 2 = active (ordinal 0) + standby (ordinal 1)
replicaCount: 1

image:
  repository: agentx-regime-swarm
  pullPolicy: IfNotPresent
  tag: ""

imagePullSecrets: []

statefulSet:
  podManagementPolicy: OrderedReady
  updateStrategy:
    type: RollingUpdate
    rollingUpdate:
      partition: 0

leaderElection:
  enabled: true
  leaderOrdinal: 0

lease:
  enabled: false
  runtimeEnabled: false
  name: regime-swarm-leader
  leaseDurationSeconds: 15
  renewIntervalSeconds: 5

service:
  headless:
    enabled: true
  metrics:
    enabled: true
    type: ClusterIP
    port: 8080
    annotations:
      prometheus.io/scrape: "true"
      prometheus.io/port: "8080"
      prometheus.io/path: "/metrics"

ingress:
  enabled: false
  className: nginx
  annotations: {}
  hosts:
    - host: regime-swarm.internal.example.com
      paths:
        - path: /metrics
          pathType: Prefix
  tls: []

persistence:
  enabled: true
  storageClassName: ""
  accessModes:
    - ReadWriteOnce
  size: 10Gi

wormPersistence:
  enabled: false
  existingClaim: ""
  mountPath: /data/worm/paper_runs
  readOnly: true

config:
  SWARM_ENV: production
  SWARM_DATA_ROOT: /data
  SWARM_CONFIG: /app/config/regime_swarm.json
  SWARM_METRICS_ENABLED: "true"
  SWARM_METRICS_PORT: "8080"
  CYCLE_INTERVAL_SECONDS: "60"
  LOG_LEVEL: INFO
  TZ: UTC
  LIVE_FEED_ENABLED: "false"
  LIVE_FEED_SYMBOL: "ETHUSDT"
  LIVE_FEED_WS_URL: "wss://stream.binance.com:9443/ws"
  LIVE_FEED_WORM_DIR: "/data/worm/live"
  SWARM_LIVE_EXECUTION: "false"

infrastructureGates:
  enabled: true
  G0_MAX_PRICE_CHANGE_PCT: 20
  G0_MAX_SPREAD_PCT: 5
  G25_MAX_LATENCY_MS: 500

smokeTest:
  enabled: false
  thresholdTest: true
  deleteHookOnSuccess: true
  wormDir: /data/worm/smoke
  auditDir: /data/audit/smoke
  summaryPath: /data/audit/pod_smoke_summary.json
  ttlSecondsAfterFinished: 600
  resources:
    limits:
      cpu: 500m
      memory: 512Mi
    requests:
      cpu: 100m
      memory: 256Mi

secrets:
  enabled: false
  slackWebhookUrl: ""
  pagerdutyIntegrationKey: ""

resources:
  limits:
    cpu: 1000m
    memory: 2Gi
  requests:
    cpu: 500m
    memory: 1Gi

terminationGracePeriodSeconds: 90

livenessProbe:
  exec:
    command:
      - /bin/sh
      - -c
      - test -f /tmp/swarm_heartbeat && find /tmp/swarm_heartbeat -mmin -2 | grep -q .
  initialDelaySeconds: 30
  periodSeconds: 30
  timeoutSeconds: 5
  failureThreshold: 3

readinessProbe:
  httpGet:
    path: /health
    port: metrics
  initialDelaySeconds: 10
  periodSeconds: 10
  timeoutSeconds: 3
  failureThreshold: 3

lifecycle:
  preStop:
    exec:
      command:
        - /bin/sh
        - -c
        - kill -TERM 1 2>/dev/null || true; sleep 20

podAnnotations: {}
podLabels: {}

nodeSelector: {}
tolerations: []
affinity: {}

serviceAccount:
  create: false
  name: ""

podDisruptionBudget:
  enabled: false
  minAvailable: 1
