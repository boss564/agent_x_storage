apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "regime-swarm.fullname" . }}-config
  labels:
    {{- include "regime-swarm.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.config }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
